import UIKit
import Social
import MobileCoreServices
import Photos
import receive_sharing_intent

class ShareViewController: RSIShareViewController {
    // Le plugin RSIShareViewController gère automatiquement la sauvegarde dans l'AppGroup
    // et la redirection vers l'application principale.
}
